{
  "name": "q",
  "version": "1.0.0",
  "description": "To make it easy for you to get started with GitLab, here's a list of recommended next steps.",
  "main": "gulpfile.js",
  "dependencies": {
    "bootstrap": "^5.1.1",
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-sass": "^5.0.0",
    "html-includes": "^4.4.1",
    "live-server": "^1.2.2",
    "sass": "^1.39.2"
  },
  "scripts": {
    "start": "live-server --port=8080 --host=0.0.0.0",
    "compile": "html-includes --src page --dest dist",
    "compile-minify": "html-includes --src page --dest dist --minify",
    "compile-watch": "html-includes --src page --dest dist --watch",
    "dev": "npm run compile && npm run start"
  },
  "author": "",
  "license": "ISC"
}
